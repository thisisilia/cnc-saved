/**
 * The iOS home-indicator safe-area band. We reserve this space at the bottom so
 * floating bars sit above where the indicator would be, but don't draw the bar
 * itself (on a real device iOS draws its own).
 */
export const HOME_INDICATOR_H = 20;
