// Native builds load fonts through expo-font, not CSS @font-face; this is a
// no-op so `import './theme/loadBrandFont'` is safe on every platform. The web
// implementation lives in loadBrandFont.web.js.
